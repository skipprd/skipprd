name: Skippr Cloud runner canary

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canary:
    name: depot canary
    runs-on: depot-ubuntu-22.04-16
    timeout-minutes: 5
    steps:
      - name: Prove GitHub Actions still starts jobs in this repo
        run: echo "workflow registered"
