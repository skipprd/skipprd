# Version 5.5.6

## Changes
- Fixed sync all pipelines functionality to properly transform date partitions
- Added caching of last run time between restarts
- Implemented automatic exit after syncing all pipelines to prevent memory leaks
- Code cleanup and optimizations

## Technical Details

### Pipeline Synchronization
- Fixed critical issue with syncing all pipelines where date partitions weren't being transformed correctly
- Implemented automatic exit after syncing all pipelines to prevent memory leaks in long-running scenarios
- Added file-based caching system to track last run time between application restarts
- Improved pipeline name handling for more reliable operation

### Performance Improvements
- Added elapsed time checking to avoid unnecessarily frequent pipeline runs
- Removed unused code and redundant configuration lookups
- Enhanced configuration caching for better performance
- Fixed environment variable cache key naming for sync frequency

### Memory Management
- Implemented preventative measures against out-of-memory (OOM) errors
- Added controlled application exit strategy after multi-pipeline operations
- Optimized resource usage during pipeline synchronization
- Improved reference handling for pipeline configurations

### Code Quality
- Simplified pipeline configuration retrieval methods
- Enhanced error handling for file operations
- Improved type consistency in pipeline name handling
- Removed unnecessary variable assignments throughout the codebase 