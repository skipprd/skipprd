name: Deadletters Test
inputs:
  architecture_name:
    description: 'macos_arm64, macos-x86, linux_x86'
    required: true
    default: 'stable'
  aws_access_key_id:
    required: true
  aws_secret_access_key:
    required: true
  docker_hub_pass:
    required: true
runs:
  using: "composite"
  steps:
    - name: Run Deadletters Test
      shell: bash
      run: |
        chmod +x ./skippr-${{ inputs.architecture_name }}/skippr
        
        cp .github/actions/e2e/deadletters/skippr.yml ./skippr.yml
        
        ls -la ./
        
        AWS_ACCESS_KEY_ID=${{ inputs.aws_access_key_id }} AWS_SECRET_ACCESS_KEY=${{ inputs.aws_secret_access_key }} AWS_DEFAULT_REGION=us-east-1 ./skippr-${{ inputs.architecture_name }}/skippr query --sql "ENABLE PIPELINE deadletters_test"
        
        AWS_ACCESS_KEY_ID=${{ inputs.aws_access_key_id }} AWS_SECRET_ACCESS_KEY=${{ inputs.aws_secret_access_key }} AWS_DEFAULT_REGION=us-east-1 ./skippr-${{ inputs.architecture_name }}/skippr --log sync --pipeline deadletters_test

#    - name: Assert deadletters exist via Skippr SQL
#      shell: bash
#      run: |
#        set -euo pipefail
#        OUT=$(AWS_DEFAULT_REGION=us-east-1 AWS_ACCESS_KEY_ID='${{ inputs.aws_access_key_id }}' AWS_SECRET_ACCESS_KEY='${{ inputs.aws_secret_access_key }}' ./skippr-${{ inputs.architecture_name }}/skippr query --sql "SELECT COUNT(*) AS c FROM deadletters WHERE namespace = 'deadletters_test'")
#        echo "$OUT"
#        COUNT=$(echo "$OUT" | grep -Eo '[0-9]+' | tail -1)
#        if [ -z "$COUNT" ] || [ "$COUNT" -lt 1 ]; then
#          echo "Expected deadletters for namespace=deadletters_test, got count=$COUNT" >&2
#          exit 1
#        fi

    - name: Asset Athena
      shell: bash
      run: |
        pip install soda-core-athena
        AWS_DEFAULT_REGION=us-east-1 AWS_ACCESS_KEY_ID=${{ inputs.aws_access_key_id }} AWS_SECRET_ACCESS_KEY=${{ inputs.aws_secret_access_key }}  soda scan -d datalake_e2e -c ./soda/configuration.yml ./soda/deadletters_test.yml
