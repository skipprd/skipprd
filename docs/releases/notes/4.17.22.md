# Version 4.17.22

## Changes

### File Handling Improvements
- Updated buffer file globbing pattern to include all merged file variants
- Enhanced pattern matching to capture files with various extensions
- Improved file identification with more flexible glob pattern
- Fixed buffer rotation to process all relevant files

### Buffer Management
- Enhanced file matching to include all buffer file variants
- Improved buffer file processing with more comprehensive pattern matching
- Fixed buffer rotation to handle files with different naming conventions
- Enhanced buffer file discovery with more flexible pattern matching

## Technical Details
- Modified `src/buffer/mod.rs` to update glob pattern from `*.merged` to `*.merged*`
- Enhanced file pattern matching to capture all buffer file variants
- Improved buffer rotation to process files with different extensions
- Fixed potential issue where some buffer files could be missed during processing
- Enhanced file discovery to ensure all relevant files are captured for processing

## Impact
This release improves the reliability of buffer file processing by ensuring that all file variants are captured during rotation. The enhanced pattern matching prevents potential data loss by including files that might have been missed with the more restrictive pattern. This change ensures more consistent and reliable data processing, particularly in scenarios with varied file naming conventions. 