name: Rust Build Release
description: 'Rust release'
inputs:
  rust-version:
    description: 'version of rust to install (e.g. stable)'
    required: true
    default: 'stable'
  architecture_name:
    description: 'macos_arm64, macos-x86, linux_x86'
    required: true
    default: 'stable'
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v3
      with:
        submodules: true
    - name: Install system dependencies (linux)
      if: runner.os == 'Linux'
      shell: bash
      run: |
        if [ "$(id -u)" = "0" ]; then
          apt-get update
          apt-get install -y protobuf-compiler cmake libcurl4-openssl-dev libssl-dev pkg-config
        else
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler cmake libcurl4-openssl-dev libssl-dev pkg-config
        fi
    - name: Install system dependencies (macos)
      if: runner.os == 'macOS'
      shell: bash
      run: |
        if ! command -v protoc >/dev/null 2>&1; then
          brew install protobuf
        fi
    - name: Setup Rust toolchain
      shell: bash
      run: |
        rustup toolchain install stable
        rustup default stable
        rustup component add rustfmt
    - name: Rust cache
      uses: Swatinem/rust-cache@v2
      with:
        shared-key: v2-release-${{ inputs.architecture_name }}
    - name: Configure version if tag commit
      shell: bash
      if: startsWith(github.ref, 'refs/tags/')
      run: |
        mv Cargo.toml Cargo.toml.orig
        sed "s/0\\.0\\.0-git/${{ github.ref_name }}/" Cargo.toml.orig > Cargo.toml
        mv Cargo.lock Cargo.lock.orig
        sed "s/0\\.0\\.0-git/${{ github.ref_name }}/" Cargo.lock.orig > Cargo.lock
    - name: Build release
      shell: bash
      run: |
        cargo build --release
        mkdir -p skippr-el-${{ inputs.architecture_name }}
        cp target/release/skippr-el skippr-el-${{ inputs.architecture_name }}/skippr-el
        ls -la skippr-el-${{ inputs.architecture_name }}
      env:
        # do not produce debug symbols to keep memory usage down
        RUSTFLAGS: "-C debuginfo=0"
    - name: Verify compiled version
      shell: bash
      run: |
        VERSION=$(target/release/skippr-el --version 2>&1 | awk '{print $2}')
        echo "skippr-el version: $VERSION"
        if [ "$VERSION" = "0.0.0-git" ] || [ -z "$VERSION" ]; then
          echo "::error::Version was not set during build"
          exit 1
        fi
    - name: Cargo Test
      shell: bash
      run: |
          cargo test --release -- --nocapture

#    - name: Verify release
#      shell: bash
#      run: |
#        AWS_DEFAULT_REGION=us-east-1 AWS_ACCESS_KEY_ID=${{secrets.AWS_ACCESS_KEY_ID}} AWS_SECRET_ACCESS_KEY=${{secrets.AWS_SECRET_ACCESS_KEY}} DATA_SOURCE_PLUGIN_NAME=s3 DATA_SOURCE_S3_BUCKET=skippr-e2e-sample-data DATA_SOURCE_S3_PREFIX=bike-hire DATA_SOURCE_BATCH_SIZE_BYTES=2048000 PIPELINE_NAME=bikehire SKIPPR_API_TOKEN=B8ib6S3wa9nSq5wAwxkO9dceUIg04d4uTYUHBDg DATA_DIR=./data APP_ENV=test target/release/skippr-el sync
#        target/release/skippr-el query 'SELECT * FROM bikehire LIMIT 10'
#        target/release/skippr-el query 'SELECT * FROM bikehire LIMIT 10' | wc -l
#        if [ $(target/release/skippr-el query 'SELECT * FROM bikehire LIMIT 10' | wc -l) -ne 10 ]; then echo "Query did not return 10 rows"; exit 1; fi
#
#        target/release/skippr-el query 'SELECT COUNT(*) FROM bikehire'
#        target/release/skippr-el query 'SELECT COUNT(*) FROM bikehire' | tail -n 1
#        if [ $(target/release/skippr-el query 'SELECT COUNT(*) FROM bikehire' | tail -n 1) -ne 5100000 ]; then echo "Query did not return 5100000 rows"; exit 1; fi
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: skippr-el-${{ inputs.architecture_name }}
        path: skippr-el-${{ inputs.architecture_name }}
        retention-days: 30